const QuizBuilder = () => {
	return (
		<main className="page-fade" style={{ padding: '32px 48px', color: 'var(--text-body)', fontFamily: 'var(--font)' }}>
			<h1 style={{ marginBottom: '8px' }}>Quiz Builder</h1>
			<p>Create and manage quiz questions for your module.</p>
		</main>
	);
};

export default QuizBuilder;
