const CourseBuilder = () => {
	return (
		<main className="page-fade" style={{ padding: '32px 48px', color: 'var(--text-body)', fontFamily: 'var(--font)' }}>
			<h1 style={{ marginBottom: '8px' }}>Course Builder</h1>
			<p>Build modules, lectures, and learning resources.</p>
		</main>
	);
};

export default CourseBuilder;
